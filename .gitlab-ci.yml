image: golang:alpine

stages:
  - build

variables:
  DOCKER_HOST: tcp://docker:2375/
  DOCKER_DRIVER: overlay2

services:
  - docker:dind

build:
  tags:
    - docker-with-registry
  stage: build
  script:
    - make build
